.introduction {
  display: flex;
  flex-direction: column;
  justify-content: center;
  height: 100%;
  padding: rem(60);

  h1 {
    font-size: rem(60);
    font-weight: 700;
  }
  h2 {
    max-width: rem(575);
    font-size: rem(60);
  }
}

.content {
  padding: rem(60);
}
